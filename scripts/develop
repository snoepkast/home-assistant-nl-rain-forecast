#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Create config dir if not present
if [[ ! -d "${PWD}/config" ]]; then
    mkdir -p "${PWD}/config"
    uv run hass --config "${PWD}/config" --script ensure_config
fi

# Make the integration importable from the dev HA config dir without a symlink.
export PYTHONPATH="${PYTHONPATH}:${PWD}/custom_components"

# Start Home Assistant under uv-managed virtualenv.
uv run hass --config "${PWD}/config" --debug
